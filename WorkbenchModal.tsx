// This file acts as a proxy to the correct WorkbenchModal component.
// It resolves a build issue caused by incorrect relative paths in a stray, old version of the file.
import WorkbenchModal from './components/WorkbenchModal';

export default WorkbenchModal;
